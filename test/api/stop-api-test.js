import { assert } from "chai";
import { assertSubset } from "../test-utils.js";
import { hikebiteService } from "./hikebite-service.js";
import { maggie, dingle, testTrails, testStops, cafe } from "../fixtures.js";

// TO DO: Add Stop tests

suite("Stop API tests", () => {
  let user = null;
  let cafe = null;

  setup(async () => {
  });

  teardown(async () => {});

  test("Create stop", async () => {
  });

  test("Create multiple stopApi", async () => {
  });

  test("Delete stop", async () => {
  });

  test("Test denormalised trail", async () => {
  });
});